/* We have parameters, which are more or less typed macros. */

param int N = 3;

export fn f () -> reg u64 {
	reg u64 res;
	reg u64[N] r;
	inline int i;

	res = 0;
	for i = 0 to N {
		r[i] = 1;
	}
	for i = 0 to N {
		res += r[i];
	}

	return res;
}
